apiVersion: v1
kind: Service
metadata:
  name: {{ include "refitdemo.fullname" . }}
  labels:
    {{- include "refitdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "refitdemo.selectorLabels" . | nindent 4 }}
